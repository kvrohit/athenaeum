body
  font-family 'Source Sans Pro', 'Ubuntu', sans-serif
  background-color #f9f9f9

/* Generic pane styles */
.mrow, .mcol { overflow: hidden; position: absolute; }
.mrow { left: 0; right: 0; }
.mcol { top: 0; bottom: 0; }
.scroll-x { overflow-x: auto; }
.scroll-y { overflow-y: auto; }

/* App specific styles */
.header.mrow { height: 43px; top: 0; }
.body.mrow { top: 43px; bottom: 0; }
// .footer.row { height: 50px; bottom: 0; }
// .left.col { width: 250px; }
// .right.col { left: 250px; right: 0; }

.navbar
  .brand
    margin-left 0

.navbar-search
  margin-right 20px

  .search-query
    line-height 2

.mosaic-block
  position relative
  float left
  width 160px
  height 230px
  overflow hidden
  margin 10px

  a
    text-decoration none

  .mosaic-overlay
    position absolute
    width 100%
    height 100%
    background-image url('../img/bg-black.png')
    opacity 0
    -moz-opacity 0
    -o-transition-duration .3s
    -moz-transition-duration .3s
    -webkit-transition-duration .3s
    z-index 5

    &:hover
      opacity 1
      -moz-opacity 1
      -o-transition-duration .3s
      -moz-transition-duration .3s
      -webkit-transition-duration .3s

    .details
      font-size 90%
      color #fff
      padding 10px

      p
        line-height 15px

  .mosaic-backdrop
    width 100%
    height 100%
    top 0

    img
      width 100%
      height 100%
